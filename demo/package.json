{
  "name": "@weft/demo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Five shapes of page, built with weft. An application, importing nothing but the framework.",
  "scripts": {
    "dev": "weft dev",
    "build": "weft build",
    "start": "weft start",
    "routes": "weft routes"
  },
  "dependencies": {
    "@weft/core": "workspace:*"
  }
}
