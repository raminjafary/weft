packages:
  - 'packages/*'
  - 'benchmarks/*'

# Nothing younger than a day is installed. A compromised or broken publish then has a
# window in which to be found before it reaches this repository. Mirrors renovate.json,
# which holds pull requests for the same period.
minimumReleaseAge: 1440
