# Exact versions only. A range is a decision deferred to whoever installs next.
save-exact=true

# The release-age gate lives in pnpm-workspace.yaml, not here: npm warns about the key and
# will reject it outright in its next major.
