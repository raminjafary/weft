# Everything on this site is meant to be found.
User-agent: *
Allow: /

# Except the two routes that are functions of their query string. Both pages are indexable; it is
# the combinations that are not, because there is no end to them and each one costs a render.
Disallow: /play?
Disallow: /search?

# Written by `weft site` from the documents it actually published, so it cannot name a page
# that does not exist or miss one that does.
Sitemap: https://weft-rr7.vercel.app/sitemap.xml
